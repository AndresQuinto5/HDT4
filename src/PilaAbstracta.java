public abstract class PilaAbstracta<E> implements iPila<E>
    {
        public boolean empty()
        {
            return size() == 0;
        }
//False si tiene un elemento
    }

    //Creamos la clase pila abstracta en de la cual van a salir todos los stacks.